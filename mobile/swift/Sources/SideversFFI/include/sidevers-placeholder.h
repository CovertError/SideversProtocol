/*
 * sidevers-placeholder.h
 *
 * The real header (sidevers.h) is copied here by build-ios.sh and the
 * matching xcframework is placed at ../../Frameworks/SideversFFI.xcframework.
 * Until then this placeholder keeps Swift Package Manager from erroring on
 * an empty include/ directory.
 *
 * To build the xcframework, run:
 *
 *   ./mobile/build-ios.sh
 *
 * from the repo root on a macOS host with Xcode CLI tools installed.
 */
